{
  "name": "findoc-analyzer",
  "version": "1.0.0",
  "description": "FinDoc Analyzer with UI Fixes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "path": "^0.12.7",
    "fs": "0.0.1-security"
  },
  "engines": {
    "node": "18.x.x"
  }
}
